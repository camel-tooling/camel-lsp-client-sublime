{
        // We start this language server when we open java or xml files.
        "selector": "source.java | text.xml",
 
        "schemes": [
        // regular files on disk
        "file"
    ]
}
 
